type IUser = {
  role: string;
  firstName: string;
  lastName: string;
  email: string;
};

type IHomeTableData = {
  user: string;
  email: string;
  reason: string;
  status: string;
  createdAt: Date;
  updatedAt: Date;
};
